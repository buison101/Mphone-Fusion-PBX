# eslint-plugin-jsx-a11y 6.10.2 still declares a peer range of eslint <=9 while the
# template pins eslint 10. The conflict is confined to the lint toolchain and does
# not affect the build, so the legacy resolver is used to keep installs repeatable.
legacy-peer-deps=true
